# Todo アプリケーション 仕様書

**バージョン**: 1.0.0  
**最終更新日**: 2024年10月18日  
**作成者**: Yucco-K

---

## 目次

1. [システム概要](#1-システム概要)
2. [機能要件](#2-機能要件)
3. [非機能要件](#3-非機能要件)
4. [画面仕様](#4-画面仕様)
5. [データ仕様](#5-データ仕様)
6. [API仕様](#6-api仕様)
7. [バリデーション仕様](#7-バリデーション仕様)
8. [エラーハンドリング](#8-エラーハンドリング)
9. [UI/UX仕様](#9-uiux仕様)

---

## 1. システム概要

### 1.1 システム名

**React Native Todo App**

### 1.2 目的

モバイルデバイス（iOS/Android）でTodoを管理するためのアプリケーション。シンプルなUIで直感的にTodoの作成・編集・削除が可能。

### 1.3 対象ユーザー

- 個人でタスク管理を行いたいユーザー
- スマートフォンでメモを取りたいユーザー

### 1.4 システム構成

```
┌─────────────────┐
│  Mobile Client  │  React Native + Expo
│  (iOS/Android)  │  NativeWind + Zod
└────────┬────────┘
         │ REST API
         │ (HTTP/JSON)
         ▼
┌─────────────────┐
│   Backend API   │  Node.js (想定)
│   Server        │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│    Database     │  (仕様外)
└─────────────────┘
```

---

## 2. 機能要件

### 2.1 機能一覧

| ID    | 機能名           | 優先度 | 実装状況 |
| ----- | ---------------- | ------ | -------- |
| F-001 | Todo作成         | 必須   | ✅ 完了  |
| F-002 | Todo一覧表示     | 必須   | ✅ 完了  |
| F-003 | Todo編集         | 必須   | ✅ 完了  |
| F-004 | Todo削除         | 必須   | ✅ 完了  |
| F-005 | バリデーション   | 必須   | ✅ 完了  |
| F-006 | トースト通知     | 必須   | ✅ 完了  |
| F-007 | 自動リフレッシュ | 必須   | ✅ 完了  |
| F-008 | ローディング表示 | 必須   | ✅ 完了  |

### 2.2 機能詳細

#### F-001: Todo作成

**概要**: 新しいTodoを作成する

**アクター**: ユーザー

**事前条件**: なし

**正常フロー**:

1. ユーザーがタイトル入力欄にテキストを入力
2. ユーザーが内容入力欄にテキストを入力
3. ユーザーが「保存」ボタンをタップ
4. システムが入力内容をバリデーション
5. バリデーション成功
6. システムがAPIにPOSTリクエストを送信
7. APIがTodoを保存
8. システムが成功トーストを表示
9. システムがフォームをクリア
10. システムがTodo一覧を再取得

**代替フロー**:

- **4a. バリデーションエラー**
  - システムがエラーメッセージを表示
  - エラートーストを表示
  - 処理を中断

- **6a. ネットワークエラー**
  - システムがエラートーストを表示
  - 処理を中断

**事後条件**: 新しいTodoが一覧に追加される

---

#### F-002: Todo一覧表示

**概要**: 登録されているTodoを一覧表示する

**アクター**: システム（自動）

**事前条件**: なし

**正常フロー**:

1. システムがAPIにGETリクエストを送信
2. APIがTodo一覧を返却
3. システムがローディングインジケーターを表示
4. システムがデータを受信
5. システムがFlatListに表示
6. ローディングインジケーターを非表示

**代替フロー**:

- **1a. ネットワークエラー**
  - システムがエラーをコンソールに出力
  - ローディングを終了

**事後条件**: Todo一覧が画面に表示される

---

#### F-003: Todo編集

**概要**: 既存のTodoを編集する

**アクター**: ユーザー

**事前条件**: Todo一覧が表示されている

**正常フロー**:

1. ユーザーがTodo項目の「編集」ボタンをタップ
2. システムが編集モーダルを表示
3. システムが既存の値をフォームに設定
4. ユーザーがタイトル/内容を編集
5. ユーザーが「保存」ボタンをタップ
6. システムが入力内容をバリデーション
7. バリデーション成功
8. システムがAPIにPUTリクエストを送信
9. APIがTodoを更新
10. システムが成功トーストを表示
11. システムがモーダルを閉じる
12. システムがTodo一覧を再取得

**代替フロー**:

- **5a. キャンセル**
  - ユーザーが「キャンセル」ボタンをタップ
  - システムがモーダルを閉じる
  - 変更を破棄

- **6a. バリデーションエラー**
  - システムがエラーメッセージを表示
  - エラートーストを表示
  - 処理を中断

**事後条件**: Todoが更新され、一覧に反映される

---

#### F-004: Todo削除

**概要**: 既存のTodoを削除する

**アクター**: ユーザー

**事前条件**: Todo一覧が表示されている

**正常フロー**:

1. ユーザーがTodo項目の「削除」ボタンをタップ
2. システムがAPIにDELETEリクエストを送信
3. APIがTodoを削除
4. システムが成功トーストを表示
5. システムがTodo一覧を再取得

**代替フロー**:

- **2a. ネットワークエラー**
  - システムがエラートーストを表示
  - 処理を中断

**事後条件**: Todoが削除され、一覧から消える

---

## 3. 非機能要件

### 3.1 パフォーマンス

| 項目           | 要件                             |
| -------------- | -------------------------------- |
| アプリ起動時間 | 3秒以内                          |
| API応答時間    | 2秒以内                          |
| 画面遷移       | 即座（ネイティブアニメーション） |
| リスト表示     | 100件まで快適に動作              |

### 3.2 ユーザビリティ

| 項目             | 要件                      |
| ---------------- | ------------------------- |
| 操作性           | タップ1回で主要操作が完了 |
| フィードバック   | 全ての操作にトースト通知  |
| ローディング表示 | 処理中は必ず表示          |
| エラーメッセージ | 日本語で分かりやすく表示  |

### 3.3 互換性

| 項目       | 要件               |
| ---------- | ------------------ |
| iOS        | iOS 13.0以上       |
| Android    | Android 5.0以上    |
| 画面サイズ | 4インチ〜6.7インチ |
| 向き       | 縦向き固定         |

### 3.4 セキュリティ

| 項目       | 要件                    |
| ---------- | ----------------------- |
| 通信       | HTTPS（本番環境）       |
| データ検証 | クライアント側でZod検証 |
| API認証    | （将来実装予定）        |

---

## 4. 画面仕様

### 4.1 メイン画面

**画面ID**: SC-001  
**画面名**: Todo管理画面

**レイアウト構成**:

```
┌─────────────────────────────┐
│    SafeArea (上部余白)       │
├─────────────────────────────┤
│  ┌─────────────────────┐    │
│  │ タイトル入力欄      │    │ ← TodoForm
│  ├─────────────────────┤    │
│  │ 内容入力欄          │    │
│  ├─────────────────────┤    │
│  │   [ 保存ボタン ]    │    │
│  └─────────────────────┘    │
├─────────────────────────────┤
│ タイトル │ 内容 │ 操作      │ ← ヘッダー
├──────────┼──────┼───────────┤
│ Todo1    │ 内容 │ 編集 削除 │ ← TodoItem
│ Todo2    │ 内容 │ 編集 削除 │
│ Todo3    │ 内容 │ 編集 削除 │
│    ↓ スクロール可能           │
└─────────────────────────────┘
```

**要素一覧**:

| ID    | 要素名         | 種類      | 説明             |
| ----- | -------------- | --------- | ---------------- |
| E-001 | タイトル入力欄 | TextInput | Todoタイトル入力 |
| E-002 | 内容入力欄     | TextInput | Todo内容入力     |
| E-003 | 保存ボタン     | Button    | Todo作成実行     |
| E-004 | ヘッダー行     | View      | 列名表示         |
| E-005 | Todoリスト     | FlatList  | Todo一覧         |
| E-006 | 編集ボタン     | Button    | 編集モーダル表示 |
| E-007 | 削除ボタン     | Button    | Todo削除実行     |

---

### 4.2 編集モーダル

**画面ID**: SC-002  
**画面名**: Todo編集モーダル

**レイアウト構成**:

```
┌─────────────────────────────┐
│      [半透明背景]            │
│   ┌─────────────────────┐   │
│   │  Todo編集           │   │
│   ├─────────────────────┤   │
│   │ タイトル入力欄      │   │
│   ├─────────────────────┤   │
│   │ 内容入力欄          │   │
│   │ (複数行)            │   │
│   ├─────────────────────┤   │
│   │ [キャンセル] [保存] │   │
│   └─────────────────────┘   │
└─────────────────────────────┘
```

**表示条件**: 編集ボタンタップ時

**閉じる条件**:

- キャンセルボタンタップ
- 保存成功
- Android戻るボタン
- 背景タップ（透過部分）

---

## 5. データ仕様

### 5.1 Todoデータ型

**型定義**:

```typescript
type Todo = {
	id: number; // 一意識別子
	title: string; // タイトル（1〜50文字）
	content: string; // 内容（1〜200文字）
};
```

**制約**:

| フィールド | 型     | 必須 | 最小値 | 最大値  | 備考         |
| ---------- | ------ | ---- | ------ | ------- | ------------ |
| id         | number | ✅   | 1      | -       | サーバー生成 |
| title      | string | ✅   | 1文字  | 50文字  | -            |
| content    | string | ✅   | 1文字  | 200文字 | -            |

### 5.2 バリデーションスキーマ

**Zodスキーマ**:

```typescript
const todoSchema = z.object({
	title: z
		.string()
		.min(1, "タイトルを入力してください")
		.max(50, "タイトルは50文字以内で入力してください"),
	content: z
		.string()
		.min(1, "内容を入力してください")
		.max(200, "内容は200文字以内で入力してください"),
});
```

---

## 6. API仕様

### 6.1 ベースURL

**開発環境**:

```
http://localhost:3000
```

**本番環境**:

```
https://api.example.com (未定)
```

### 6.2 共通仕様

**リクエストヘッダー**:

```http
Content-Type: application/json
Accept: application/json
```

**レスポンス形式**: JSON

**エラーレスポンス**:

```json
{
	"error": {
		"code": "ERROR_CODE",
		"message": "エラーメッセージ"
	}
}
```

---

### 6.3 エンドポイント詳細

#### 6.3.1 Todo一覧取得

**エンドポイント**: `GET /api/todos`

**リクエスト**:

```http
GET /api/todos HTTP/1.1
Host: localhost:3000
```

**レスポンス (200 OK)**:

```json
[
	{
		"id": 1,
		"title": "タスク1",
		"content": "タスク1の内容です。"
	},
	{
		"id": 2,
		"title": "タスク2",
		"content": "タスク2の内容です。"
	}
]
```

**エラーレスポンス (500 Internal Server Error)**:

```json
{
	"error": {
		"code": "INTERNAL_ERROR",
		"message": "サーバーエラーが発生しました"
	}
}
```

---

#### 6.3.2 Todo作成

**エンドポイント**: `POST /api/todos`

**リクエスト**:

```http
POST /api/todos HTTP/1.1
Host: localhost:3000
Content-Type: application/json

{
  "title": "新しいタスク",
  "content": "タスクの内容"
}
```

**レスポンス (201 Created)**:

```json
{
	"id": 3,
	"title": "新しいタスク",
	"content": "タスクの内容"
}
```

**エラーレスポンス (400 Bad Request)**:

```json
{
	"error": {
		"code": "VALIDATION_ERROR",
		"message": "入力内容が不正です"
	}
}
```

---

#### 6.3.3 Todo更新

**エンドポイント**: `PUT /api/todos/:id`

**リクエスト**:

```http
PUT /api/todos/1 HTTP/1.1
Host: localhost:3000
Content-Type: application/json

{
  "title": "更新後のタイトル",
  "content": "更新後の内容"
}
```

**レスポンス (200 OK)**:

```json
{
	"id": 1,
	"title": "更新後のタイトル",
	"content": "更新後の内容"
}
```

**エラーレスポンス (404 Not Found)**:

```json
{
	"error": {
		"code": "NOT_FOUND",
		"message": "Todoが見つかりません"
	}
}
```

---

#### 6.3.4 Todo削除

**エンドポイント**: `DELETE /api/todos/:id`

**リクエスト**:

```http
DELETE /api/todos/1 HTTP/1.1
Host: localhost:3000
```

**レスポンス (204 No Content)**:

```
(空のレスポンス)
```

**エラーレスポンス (404 Not Found)**:

```json
{
	"error": {
		"code": "NOT_FOUND",
		"message": "Todoが見つかりません"
	}
}
```

---

## 7. バリデーション仕様

### 7.1 クライアント側バリデーション

**実行タイミング**: 保存ボタンタップ時

**バリデーション項目**:

| フィールド | ルール      | エラーメッセージ                         |
| ---------- | ----------- | ---------------------------------------- |
| title      | 必須        | "タイトルを入力してください"             |
| title      | 最小1文字   | "タイトルを入力してください"             |
| title      | 最大50文字  | "タイトルは50文字以内で入力してください" |
| content    | 必須        | "内容を入力してください"                 |
| content    | 最小1文字   | "内容を入力してください"                 |
| content    | 最大200文字 | "内容は200文字以内で入力してください"    |

**表示方法**:

- 各入力欄の下に赤文字でエラーメッセージ表示
- トーストで「入力エラー - 入力内容を確認してください」表示

---

## 8. エラーハンドリング

### 8.1 エラー種別と対応

| エラー種別           | 検知タイミング   | 対応                              |
| -------------------- | ---------------- | --------------------------------- |
| バリデーションエラー | 保存時           | エラーメッセージ表示、処理中断    |
| ネットワークエラー   | API呼び出し時    | エラートースト表示                |
| APIエラー (4xx)      | レスポンス受信時 | エラートースト表示                |
| APIエラー (5xx)      | レスポンス受信時 | エラートースト表示                |
| 予期せぬエラー       | 任意             | console.error出力、エラートースト |

### 8.2 トースト仕様

**表示位置**: 画面上部（`topOffset: 60`）

**表示時間**: 3秒（自動消去）

**種類**:

| 種類    | 背景色 | アイコン | 使用場面                              |
| ------- | ------ | -------- | ------------------------------------- |
| success | 緑     | ✓        | 作成/更新/削除成功                    |
| error   | 赤     | ✗        | バリデーション/API/ネットワークエラー |

**メッセージ一覧**:

| 操作           | 成功メッセージ                  | エラーメッセージ                          |
| -------------- | ------------------------------- | ----------------------------------------- |
| 作成           | "保存成功 - Todoを保存しました" | "保存失敗 - Todoの保存に失敗しました"     |
| 更新           | "更新成功 - Todoを更新しました" | "更新失敗 - Todoの更新に失敗しました"     |
| 削除           | "削除成功 - Todoを削除しました" | "削除失敗 - Todoの削除に失敗しました"     |
| バリデーション | -                               | "入力エラー - 入力内容を確認してください" |

---

## 9. UI/UX仕様

### 9.1 カラーパレット

| 用途           | Tailwind Class        | Hex Code | 説明                 |
| -------------- | --------------------- | -------- | -------------------- |
| プライマリ     | `bg-black`            | #000000  | 保存ボタン           |
| セカンダリ     | `bg-blue-500`         | #3B82F6  | 編集ボタン           |
| 危険           | `bg-red-500`          | #EF4444  | 削除ボタン           |
| グレー         | `bg-gray-300`         | #D1D5DB  | キャンセルボタン     |
| エラーテキスト | `text-red-500`        | #EF4444  | バリデーションエラー |
| ボーダー       | `border-gray-300/400` | #D1D5DB  | 入力欄、テーブル     |

### 9.2 タイポグラフィ

**フォント**: Noto Sans JP

| 要素                 | ウェイト      | サイズ | Tailwind Class           |
| -------------------- | ------------- | ------ | ------------------------ |
| 見出し               | Bold (700)    | xl     | `text-xl font-noto-bold` |
| ボタンテキスト       | Bold (700)    | base   | `font-noto-bold`         |
| 通常テキスト         | Regular (400) | base   | `font-noto-regular`      |
| エラーメッセージ     | Regular (400) | sm     | `text-sm`                |
| ボタンテキスト（小） | Bold (700)    | xs     | `text-xs font-noto-bold` |

### 9.3 スペーシング

**基本単位**: 4px (Tailwind rem単位)

| 用途             | Tailwind Class | 実測値    |
| ---------------- | -------------- | --------- |
| コンポーネント間 | `mb-2`, `mb-4` | 8px, 16px |
| パディング（小） | `p-2`          | 8px       |
| パディング（中） | `p-4`          | 16px      |
| パディング（大） | `p-6`          | 24px      |
| 横並び間隔       | `space-x-2`    | 8px       |
| 縦並び間隔       | `space-y-2`    | 8px       |

### 9.4 ボタン仕様

**サイズ**:

| ボタン種類            | パディング  | 角丸         | Tailwind Class |
| --------------------- | ----------- | ------------ | -------------- |
| 大（保存）            | `p-2`       | `rounded-md` | 8px / 6px      |
| 中（編集/削除）       | `px-2 py-1` | `rounded-md` | 8px×4px / 6px  |
| 中（キャンセル/保存） | `px-4 py-2` | `rounded-md` | 16px×8px / 6px |

**状態**:

| 状態     | 透明度 | カーソル    | 説明                  |
| -------- | ------ | ----------- | --------------------- |
| 通常     | 100%   | pointer     | -                     |
| タップ中 | 50%    | pointer     | `activeOpacity={0.5}` |
| 無効     | -      | not-allowed | `disabled={true}`     |

### 9.5 アニメーション

| 要素         | アニメーション | 時間  |
| ------------ | -------------- | ----- |
| モーダル表示 | fade           | 300ms |
| トースト表示 | slide (上から) | 300ms |
| ボタンタップ | opacity変化    | 即座  |

### 9.6 レスポンシブ設計

**レイアウト幅**:

| 要素       | 幅          | Tailwind Class |
| ---------- | ----------- | -------------- |
| タイトル列 | 2/6 (33.3%) | `w-2/6`        |
| 内容列     | 2/6 (33.3%) | `w-2/6`        |
| 操作列     | 2/6 (33.3%) | `w-2/6`        |
| モーダル   | 5/6 (83.3%) | `w-5/6`        |

**SafeArea対応**:

- iOS: ノッチ、ホームインジケーター回避
- Android: ステータスバー、ナビゲーションバー回避
- コンポーネント: `<SafeAreaView>` 使用

---

## 10. 制約事項

### 10.1 技術的制約

| 項目           | 制約内容                 |
| -------------- | ------------------------ |
| オフライン動作 | 非対応（ネット必須）     |
| データ永続化   | ローカルストレージ未使用 |
| 認証機能       | 未実装                   |
| 多言語対応     | 日本語のみ               |
| ダークモード   | 未対応                   |

### 10.2 運用上の制約

| 項目               | 制約内容               |
| ------------------ | ---------------------- |
| Todo件数上限       | クライアント側制限なし |
| 同時編集           | 非対応                 |
| データバックアップ | サーバー側依存         |
| データエクスポート | 未実装                 |

---

## 11. 今後の拡張予定

### 11.1 短期（v1.1）

- [ ] Todo完了/未完了フラグ
- [ ] Todo検索機能
- [ ] Todoソート機能
- [ ] プルtoリフレッシュ

### 11.2 中期（v1.2）

- [ ] カテゴリ機能
- [ ] 期限設定
- [ ] 優先度設定
- [ ] ダークモード対応

### 11.3 長期（v2.0）

- [ ] ユーザー認証
- [ ] データ同期
- [ ] オフライン対応
- [ ] 共有機能

---

## 付録

### A. 用語集

| 用語           | 説明                                                 |
| -------------- | ---------------------------------------------------- |
| Todo           | タスク、やるべきことを記録したアイテム               |
| CRUD           | Create(作成), Read(読取), Update(更新), Delete(削除) |
| トースト       | 画面上部に表示される短時間の通知                     |
| バリデーション | 入力値の妥当性チェック                               |
| モーダル       | 画面上に重ねて表示されるダイアログ                   |

### B. 参考資料

- [Expo Documentation](https://docs.expo.dev/)
- [React Native Documentation](https://reactnative.dev/)
- [NativeWind Documentation](https://www.nativewind.dev/)
- [Zod Documentation](https://zod.dev/)

---

**文書履歴**:

| バージョン | 日付       | 変更内容 | 作成者  |
| ---------- | ---------- | -------- | ------- |
| 1.0.0      | 2024-10-18 | 初版作成 | Yucco-K |
