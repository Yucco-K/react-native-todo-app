# Firebase メール認証・パスワードリセット トラブルシューティング

このガイドでは、メール認証とパスワードリセットが正常に動作しない場合の確認手順を説明します。

---

## 🔍 確認手順

### ステップ1: Firebase Console の基本設定を確認

1. **Firebase Console** を開く
   - https://console.firebase.google.com/

2. **Authentication** → **Sign-in method** タブ
   - ✅ **Email/Password** が「有効」になっているか確認
   - 無効の場合は、クリックして有効化

3. **Authentication** → **Settings** タブ
   - **Authorized domains** に以下が含まれているか確認：
     - `localhost`
     - `*.firebaseapp.com`
     - `*.web.app`

---

### ステップ2: メールテンプレートを確認

1. **Authentication** → **Templates** タブ

2. **Email address verification** を選択
   - デフォルトのテンプレートが設定されているか確認
   - カスタマイズする場合は `FIREBASE_EMAIL_SETUP.md` を参照

3. **Password reset** を選択
   - デフォルトのテンプレートが設定されているか確認

---

### ステップ3: 実際にテストする

#### 新規アカウント登録のテスト

1. TestFlightアプリを開く
2. 「新規登録」をタップ
3. **テスト用メールアドレス**を入力（例: `test@example.com`）
4. パスワードを入力（例: `Test1234!`）
5. 「新規登録」ボタンをタップ

**期待される動作**:
- ✅ 「認証メールを送信しました」のアラートが表示される
- ✅ ログイン画面に戻る
- ✅ 数分以内にメールが届く（迷惑メールフォルダも確認）

**メールが届かない場合**:
1. Firebase Console → **Authentication** → **Users** タブ
2. 登録したメールアドレスが表示されているか確認
3. 表示されている場合は、Firebase側でメールが送信されている
4. メールプロバイダー（Gmail, iCloud等）の迷惑メールフォルダを確認

#### パスワードリセットのテスト

1. ログイン画面で「パスワードを忘れた場合」をタップ
2. 登録済みのメールアドレスを入力
3. 「送信」ボタンをタップ

**期待される動作**:
- ✅ 「送信完了」のトーストが表示される
- ✅ ログイン画面に戻る
- ✅ 数分以内にメールが届く

---

## 🐛 よくある問題と解決方法

### 問題1: メールが届かない

**原因1**: Firebase Console で Email/Password が無効
- **解決**: ステップ1 を確認

**原因2**: メールプロバイダーがブロックしている
- **解決**: 迷惑メールフォルダを確認
- **解決**: `noreply@<project-id>.firebaseapp.com` を許可リストに追加

**原因3**: Firebase の無料枠を超えている
- **解決**: Firebase Console → **Usage** タブで確認
- 無料枠: 1日100通まで

### 問題2: メールのリンクをクリックしても何も起こらない

**原因**: ブラウザで認証ページが開くが、アプリに戻らない
- **解決**: これは**正常な動作**です
- メール認証とパスワードリセットは**ブラウザで完了**します
- 完了後、アプリに戻ってログインしてください

### 問題3: 「このメールアドレスは登録されていません」エラー

**原因**: パスワードリセットで存在しないメールアドレスを入力
- **解決**: 正しいメールアドレスを入力
- **解決**: Firebase Console → **Authentication** → **Users** で登録済みメールアドレスを確認

### 問題4: 「メール認証が必要です」エラーでログインできない

**原因**: メールのリンクをクリックしていない
- **解決**: メールを確認し、「メールアドレスを確認」リンクをクリック
- **解決**: ブラウザで認証ページが開き、「Your email has been verified」と表示されればOK
- **解決**: アプリに戻ってログイン

---

## 📧 メール送信フロー（技術詳細）

### 新規アカウント登録

```
1. ユーザーがメールアドレスとパスワードを入力
   ↓
2. Firebase Authentication にユーザーを作成
   ↓
3. sendEmailVerification() を実行
   ↓
4. Firebase がメールを送信（noreply@<project-id>.firebaseapp.com から）
   ↓
5. ユーザーが自動的にログアウト
   ↓
6. ユーザーがメールのリンクをクリック
   ↓
7. ブラウザで Firebase の認証ページが開く
   ↓
8. Firebase Auth の emailVerified が true に更新
   ↓
9. アプリに戻ってログイン
   ↓
10. onAuthStateChanged で emailVerified を検知
   ↓
11. Firestore の emailVerified も true に更新
   ↓
12. ログイン成功
```

### パスワードリセット

```
1. ユーザーがメールアドレスを入力
   ↓
2. sendPasswordResetEmail() を実行
   ↓
3. Firebase がメールを送信
   ↓
4. ユーザーがメールのリンクをクリック
   ↓
5. ブラウザで Firebase のパスワードリセットページが開く
   ↓
6. ユーザーが新しいパスワードを入力
   ↓
7. Firebase Auth のパスワードが更新
   ↓
8. アプリに戻ってログイン
   ↓
9. ログイン成功
```

---

## 🔧 デバッグ方法

### コンソールログを確認

TestFlightアプリでテストする際、以下のログが表示されるはずです：

#### 新規登録時
```
✅ ユーザー作成成功: { userId: "...", email: "..." }
✅ ユーザー情報をFirestoreに保存しました
✅ 認証メールを送信しました: test@example.com
✅ メール認証待ちのためログアウトしました
```

#### パスワードリセット時
```
📧 パスワードリセットメールを送信中... test@example.com
✅ パスワードリセットメールを送信しました: test@example.com
```

### Firebase Console でユーザーを確認

1. **Authentication** → **Users** タブ
2. 登録したユーザーが表示されているか確認
3. **Email verified** 列が `Yes` になっているか確認

---

## 📞 サポート

それでも問題が解決しない場合は、以下の情報を添えてお問い合わせください：

1. エラーメッセージ（スクリーンショット）
2. コンソールログ（スクリーンショット）
3. Firebase Console の設定（スクリーンショット）
4. 使用しているメールプロバイダー（Gmail, iCloud等）

サポート: yukig0527@icloud.com

